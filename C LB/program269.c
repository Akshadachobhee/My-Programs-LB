//Template for Doubly Linked List
#include<stdio.h>
#include<stdlib.h>

struct Node
{
    int data;
    struct Node *next;
    struct Node *prev; //#
    
} NODE,*PNODE,**PPNODE;

int main()
{
    PNODE First = NULL;

    return 0;
}